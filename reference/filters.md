# parses the maths-like syntax of filters.

parses the maths-like syntax of filters.

## Usage

``` r
filters(x)
```

## Arguments

- x:

  the filters string to parse

## Value

the download URL as a string
