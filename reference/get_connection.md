# get the default connection if any

get the default connection if any

## Usage

``` r
get_connection(auto = TRUE)
```

## Arguments

- auto:

  whether to automatically use autoconnect() if the default connection
  is not yet set

## See also

set_connection
