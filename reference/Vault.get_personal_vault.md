# Vault.get_personal_vault

Retrieves the current users"s personal, private vault.

## Usage

``` r
Vault.get_personal_vault(env = get_connection())
```

## Arguments

- env:

  (optional) Custom client environment.

## Examples

``` r
if (FALSE) { # \dontrun{
Vault.get_personal_vault()
} # }
```
