# fetches user information.

fetches user information.

## Usage

``` r
User(conn = get_connection())
```

## Arguments

- conn:

  a EDP connection object (as a named list or environment)

## Value

the connected user information as a User object
